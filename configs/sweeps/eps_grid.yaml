# Example sweep grid: constraint tolerance x dual step size.
# eps is in squared-value units (tolerated value gap ~ sqrt(eps)).
# python -m experiments.sweep --base configs/gridworld_exact.yaml \
#     --grid configs/sweeps/eps_grid.yaml --seeds 0 1 2
trainer.eps: [0.0004, 0.0025, 0.01]   # value gaps ~ 0.02, 0.05, 0.10
duals.lr: [1.0, 3.0, 8.0]
